# Attack Detection 

## Instructions

1. ## git add . ## <-- Stages everything in the current directory.
2. ## git commit -m "first commit" ## <-- Saves with a comment.
3. ## git push origin <branch_name> ## <-- Push to repository (main is the master branch)
